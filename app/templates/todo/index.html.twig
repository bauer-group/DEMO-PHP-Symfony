{% extends 'base.html.twig' %}

{% block title %}My Todos - PHP Todo App{% endblock %}

{% block body %}
    <!-- Statistics -->
    <div class="row mb-4">
        <div class="col-6 col-md-3">
            <div class="stat-card">
                <div class="stat-number">{{ stats.total }}</div>
                <div class="text-muted">Total</div>
            </div>
        </div>
        <div class="col-6 col-md-3">
            <div class="stat-card">
                <div class="stat-number">{{ stats.pending }}</div>
                <div class="text-muted">Pending</div>
            </div>
        </div>
        <div class="col-6 col-md-3">
            <div class="stat-card success">
                <div class="stat-number">{{ stats.completed }}</div>
                <div class="text-muted">Completed</div>
            </div>
        </div>
        <div class="col-6 col-md-3">
            <div class="stat-card danger">
                <div class="stat-number">{{ stats.overdue }}</div>
                <div class="text-muted">Overdue</div>
            </div>
        </div>
    </div>

    <!-- Todos List -->
    <div class="card">
        <div class="card-header d-flex justify-content-between align-items-center">
            <h5 class="mb-0">
                <i class="bi bi-list-check me-2"></i>My Todos
            </h5>
            <a href="{{ path('todo_new') }}" class="btn btn-gradient-primary btn-sm">
                <i class="bi bi-plus-lg me-1"></i>Add Todo
            </a>
        </div>
        <div class="card-body p-0">
            {% if todos is empty %}
                <div class="empty-state">
                    <i class="bi bi-inbox"></i>
                    <h4>No todos yet</h4>
                    <p class="text-muted mb-4">Get started by creating your first todo!</p>
                    <a href="{{ path('todo_new') }}" class="btn btn-gradient-primary">
                        <i class="bi bi-plus-lg me-2"></i>Create Todo
                    </a>
                </div>
            {% else %}
                <div class="list-group list-group-flush">
                    {% for todo in todos %}
                        <div class="list-group-item todo-item priority-{{ todo.priorityLabel|lower }}{% if todo.completed %} completed{% endif %}{% if todo.overdue %} overdue{% endif %}">
                            <div class="d-flex justify-content-between align-items-start">
                                <div class="d-flex align-items-start flex-grow-1">
                                    <form action="{{ path('todo_toggle', {id: todo.id}) }}" method="post" class="me-3">
                                        <input type="hidden" name="_token" value="{{ csrf_token('toggle' ~ todo.id) }}">
                                        <button type="submit" class="btn btn-link p-0" title="Toggle completion">
                                            {% if todo.completed %}
                                                <i class="bi bi-check-circle-fill text-success fs-4"></i>
                                            {% else %}
                                                <i class="bi bi-circle text-muted fs-4"></i>
                                            {% endif %}
                                        </button>
                                    </form>
                                    <div class="flex-grow-1">
                                        <h6 class="todo-title mb-1">{{ todo.title }}</h6>
                                        {% if todo.description %}
                                            <p class="text-muted mb-2 small">{{ todo.description|slice(0, 150) }}{% if todo.description|length > 150 %}...{% endif %}</p>
                                        {% endif %}
                                        <div class="d-flex align-items-center gap-2 flex-wrap">
                                            <span class="priority-badge priority-{{ todo.priorityLabel|lower }}">
                                                {{ todo.priorityLabel }}
                                            </span>
                                            {% if todo.dueDate %}
                                                <span class="due-date{% if todo.overdue %} overdue{% endif %}">
                                                    <i class="bi bi-calendar-event me-1"></i>
                                                    {{ todo.dueDate|date('M d, Y') }}
                                                    {% if todo.overdue %}
                                                        <span class="badge bg-danger ms-1">Overdue</span>
                                                    {% endif %}
                                                </span>
                                            {% endif %}
                                            <span class="text-muted small">
                                                <i class="bi bi-clock me-1"></i>
                                                {{ todo.createdAt|date('M d, Y H:i') }}
                                            </span>
                                        </div>
                                    </div>
                                </div>
                                <div class="todo-actions d-flex gap-2">
                                    <a href="{{ path('todo_edit', {id: todo.id}) }}" class="btn btn-outline-primary btn-sm" title="Edit">
                                        <i class="bi bi-pencil"></i>
                                    </a>
                                    <form action="{{ path('todo_delete', {id: todo.id}) }}" method="post" class="d-inline" onsubmit="return confirm('Are you sure you want to delete this todo?');">
                                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ todo.id) }}">
                                        <button type="submit" class="btn btn-outline-danger btn-sm" title="Delete">
                                            <i class="bi bi-trash"></i>
                                        </button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            {% endif %}
        </div>
    </div>
{% endblock %}
